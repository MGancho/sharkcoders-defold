function init(self)
	gui.set_text(gui.get_node("life"), "LIVES: 3")
end

function on_message(self, message_id, message, sender)
	if message_id == hash("update_life") then
		gui.set_text(gui.get_node("item"), "LIVES: " .. message.value)
	end
end
